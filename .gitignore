# General compiled files and build output
*.class                 # Java compiled class files
target/                 # Standard Maven build output directory
build/                  # Standard Gradle build output directory
bin/                    # Common output directory for simpler projects

# Package and archive files
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# Log files
*.log                   # General log files
hs_err_pid* # JVM crash logs
replay_pid* # JVM replay logs

# Maven Wrapper files (if your project uses Maven Wrapper)
!.mvn/wrapper/maven-wrapper.jar # Keep the wrapper JAR (it's part of the project setup)

# IDE-specific ignores (JetBrains - IntelliJ IDEA)
# Note: Some of these .idea/ files *might* be committed if shared project settings are desired.
# But for personal workspace settings, they should be ignored.
.idea/workspace.xml
.idea/tasks.xml
.idea/usage.statistics.xml
.idea/dictionaries/
.idea/shelf/
.idea/dataSources/
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/webServers.xml
.idea/jsLibraryMappings.xml
.idea/httpRequests/
.idea/inspectionProfiles/profiles_settings.xml # Local inspection settings
*.iws                    # Old IntelliJ workspace file
*.iml                    # IntelliJ module file (often ignored if using Maven/Gradle)
*.ipr                    # Old IntelliJ project file

# IDE-specific ignores (Eclipse)
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

# IDE-specific ignores (NetBeans)
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
!**/src/main/**/build/ # Keep nested builds for specific cases if needed (from original)
!**/src/test/**/build/  # Keep nested builds for specific cases if needed (from original)

# IDE-specific ignores (VS Code)
.vscode/

# Operating System specific files
.DS_Store               # macOS specific
Thumbs.db               # Windows specific

# Other tool-specific ignores
*.ctxt                  # BlueJ files
.mtj.tmp/               # Mobile Tools for Java temporary files

# Optional: If you *only* want to ignore .idea/workspace.xml and keep other .idea files:
# Then replace all the specific .idea/ ignores above with just:
# .idea/workspace.xml
# This means that files like .idea/modules.xml, .idea/misc.xml, etc. would still be tracked.
# For most teams, it's common to track modules.xml, misc.xml, vcs.xml for consistent project setup.
# If you are the only developer or don't care about sharing IDE settings, ignoring the whole .idea/
# (with the ! exclusions) is simpler.